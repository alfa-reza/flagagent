{
  "identity": "local-marker",
  "description": "Connect to target:9999 with a TCP client. Read the marker, remove the suffix -ok, replace hyphens with underscores, wrap the result as Flag{...}, and submit it.",
  "expected_flag": "Flag{flagagent_target}",
  "network_mode": "local",
  "target_context": "The audited local target is reachable at target:9999. It returns one deterministic marker per connection."
}
